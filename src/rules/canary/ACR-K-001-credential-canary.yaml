id: ACR-K-001
version: "1.0.0"
title: "Credential canary token exfiltrated"
status: disabled
created: "2026-03-19"
modified: "2026-03-19"
author: "AgentCanary Core"
confidence: high
severity: critical
category: credential_harvesting
subcategory: canary-token
description: >
  Detects when a planted credential canary token (e.g. fake AWS key,
  API token) is accessed, transmitted, or referenced by a tool or agent.
  This indicates the tool is actively harvesting credentials from its
  environment. Placeholder rule — canary infrastructure not yet implemented.
tags:
  - canary
  - credential
  - exfiltration
  - placeholder
mitre:
  tactics:
    - credential-access
    - exfiltration
  techniques:
    - T1552.001
    - T1041
applies_to:
  - mcp_server
  - skill_file
  - tool_definition
detection:
  check_type: static_pattern
  patterns:
    - id: credential_canary_placeholder
      description: "Placeholder — will be replaced by canary runtime check"
      type: string
      value: "__ACR_CREDENTIAL_CANARY_PLACEHOLDER__"
      target: file_content
  condition: any
report:
  title: "Credential canary token was accessed"
  recommendation: >
    A planted fake credential was accessed by this tool. This is strong
    evidence of credential harvesting behavior. Do not grant this tool
    access to any real credentials or sensitive environment variables.
false_positives:
  - "None expected — canary tokens are never used in legitimate code"
