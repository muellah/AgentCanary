id: ACR-K-003
version: "1.0.0"
title: "Network canary token contacted"
status: disabled
created: "2026-03-19"
modified: "2026-03-19"
author: "AgentCanary Core"
confidence: high
severity: critical
category: data_exfiltration
subcategory: canary-token
description: >
  Detects when a planted canary URL receives a request from a tool or
  agent. The canary URL is unique per scan and should never be contacted
  unless the tool is making unauthorized network requests or exfiltrating
  data. Placeholder rule — canary infrastructure not yet implemented.
tags:
  - canary
  - network
  - exfiltration
  - placeholder
mitre:
  tactics:
    - exfiltration
    - command-and-control
  techniques:
    - T1041
    - T1071.001
applies_to:
  - mcp_server
  - skill_file
  - tool_definition
detection:
  check_type: static_pattern
  patterns:
    - id: network_canary_placeholder
      description: "Placeholder — will be replaced by canary runtime check"
      type: string
      value: "__ACR_NETWORK_CANARY_PLACEHOLDER__"
      target: file_content
  condition: any
report:
  title: "Network canary URL was contacted"
  recommendation: >
    A planted canary URL received a request from this tool. This is strong
    evidence of unauthorized network activity. Block this tool's network
    access and investigate what data may have been transmitted.
false_positives:
  - "None expected — canary URLs are unique and never appear in legitimate traffic"
